apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-myapp-svc
spec:
  selector:
    app: {{ .Release.Name }}-myapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.service.port }}